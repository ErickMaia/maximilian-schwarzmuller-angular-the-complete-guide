import { RevealedMessageDirective } from './revealed-message.directive';

describe('RevealedMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new RevealedMessageDirective();
    expect(directive).toBeTruthy();
  });
});
